import { FlipImagesDirective } from './flip-images.directive';

describe('FlipImagesDirective', () => {
  it('should create an instance', () => {
    const directive = new FlipImagesDirective();
    expect(directive).toBeTruthy();
  });
});
